var assert = require('assert');
var recognitor = require('../../../apps/cut/modules/cutEndsGenerator.js')();

describe('Cut ends generator', function () {
    describe('get ends method', function () {
        xit('should return cut ends', function (done) {
            done();
        });
    });
});